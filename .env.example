# Database Configuration
# Choose one of the following:

# PostgreSQL (default for production)
DATABASE_URL=postgresql+asyncpg://fastauth:fastauth@localhost:5432/fastauth

# SQLite (file-based, good for development)
# DATABASE_URL=sqlite+aiosqlite:///./fastauth.db

# SQLite (in-memory, good for testing)
# DATABASE_URL=sqlite+aiosqlite:///:memory:

# JWT Configuration
SECRET_KEY=your-super-secret-key-change-this-in-production-must-be-long-and-random
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Application Configuration
APP_NAME=FastAuth
DEBUG=false

# Docker Compose (PostgreSQL service)
# DATABASE_URL=postgresql+asyncpg://fastauth:fastauth@postgres:5432/fastauth

# Generate a secure secret key using:
# python -c "import secrets; print(secrets.token_urlsafe(32))"